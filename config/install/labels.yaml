apiVersion: builtin
kind: LabelTransformer
metadata:
  name: labels
labels:
  app.kubernetes.io/part-of: flux-kluctl-controller
  app.kubernetes.io/instance: flux-system
fieldSpecs:
  - path: metadata/labels
    create: true
